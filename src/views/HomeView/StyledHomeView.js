import styled from "styled-components";

export const StyledHomeView = styled.div((prop) => ({
  display: "flex",
  flexWrap: "wrap",
  justifyContent: "center",
  alignItems: "center",
  padding: "0px 19px",
}));
